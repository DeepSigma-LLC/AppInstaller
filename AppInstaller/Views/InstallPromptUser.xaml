<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AppInstaller.Views.InstallPromptUser"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppInstaller.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20, 0, 20 ,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Please select the location to install the application." Padding="0, 0, 0, 10"/>
        
        <Grid Grid.Row="1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="selectedFilePath" Grid.Column="0" PlaceholderText="Selected Install Location:" HorizontalAlignment="Stretch"/>
            <TextBlock Grid.Column="1" Padding="5,0,0,0"/>
            <Button Grid.Column="2" Content="Select Location" Click="Select_Folder_Click"/>
        </Grid>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" Padding="0, 20, 0, 0"  Spacing="10" HorizontalAlignment="Center">
            <Button Content="Install" Style="{StaticResource AccentButtonStyle}" Click="Install_Button_Click"/>
            <Button Content="Cancel" Click="Cancel_Button_Click"/>
        </StackPanel>
    </Grid>
</Page>
